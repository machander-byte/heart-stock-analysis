name: heartanalysis
description: "A new Flutter project for heart stroke prediction."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Backend and utility packages for a full application
  http: ^1.2.1
  shared_preferences: ^2.2.2
  pdf: ^3.10.8
  printing: ^5.13.1

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
